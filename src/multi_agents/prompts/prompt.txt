<CLS>       # 开始标记，用于表示对话或文本的开始
<POS>       # 位置标记，用于表示文本中的位置信息
<>          # 占位符，用于分隔或标记空白区域
<USER>      # 用户输入标记，用于表示用户的发言
<ASSISTANT> # 模型生成的文本标记，用于表示模型的回答
<RESPONSE>  # 回答标记，用于明确模型的回答部分
<QUESTION>  # 问题标记，用于标记提问内容
<ANSWER>    # 回答标记，用于标记回答内容
<INFO>      # 信息标记，用于标记额外的背景信息
<EXAMPLE>   # 示例标记，用于标记示例部分
<DETAIL>    # 详细信息标记，用于提供更多细节
<ERROR>     # 错误标记，用于标记错误或异常
<TASK>      # 任务标记，用于标记具体的任务或目标
<CONTEXT>   # 上下文标记，用于提供额外的上下文信息
<NOTE>      # 注释标记，用于添加说明性注释
<...>       # 其他标记，根据需要扩展

# Nodes
A:Page Load
B:Request Hot 100 Problems
C:Backend Returns Hot 100 Problems
D:Display Hot 100 Problems
E:User Clicks on a Problem
F:Request Problem Details
G:Backend Returns Problem Details
H:Display Problem Details
I:User Applies Filters or Searches
J:Request Filtered/Search Results
K:Backend Returns Filtered/Search Results
L:Update Displayed Problem List

# Edges
A -> B
B -> C
C -> D
E -> F
F -> G
G -> H
I -> J
J -> K
K -> L

syntax = "proto3";

package leetcode;

message Problem {
  int32 id = 1;
  string title = 2;
  string url = 3;
  int32 difficulty = 4; // 1 for Easy, 2 for Medium, 3 for Hard
}